# Copy to backend/.env on your VM before running setup-vm.sh
HOST=0.0.0.0
PORT=8080

# Your Vercel frontend URL (comma-separated for preview + production)
CORS_ORIGINS=https://your-app.vercel.app

CACHE_TTL_SECONDS=300
MIN_RISK_REWARD=1.5
