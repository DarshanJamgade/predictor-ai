# Local dev — copy to frontend/.env.local
VITE_API_URL=http://localhost:8080

# Vercel full-stack: leave VITE_API_URL unset (prod uses /_/backend automatically).
# Do NOT set VITE_API_URL=http://localhost:8080 on Vercel.

# Vercel frontend + VM backend:
# VITE_API_URL=https://api.yourdomain.com
